pipeline {
  agent any

  stages {
    stage ('cloning the code'){
      steps
      {
        sh 'git clone https://github.com/git-vikki/friday.git'
      }
    }
    stage ('build'){
      steps
      {
        sh 
      }
    }
  }
}
